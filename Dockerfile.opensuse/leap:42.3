FROM opensuse/leap:42.3
MAINTAINER opensuse

RUN zypper -n up && zypper -n in wget && zypper -n in -t pattern devel_C_C++ devel_basis devel_rpm_build
RUN groupadd mock
RUN useradd -m -d /home/user -s /bin/bash user && usermod -a -G mock user
USER user
WORKDIR /home/user

